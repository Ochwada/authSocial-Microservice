services:
  auth-service:
    build: .
    container_name: auth-service
    ports:
      - "9090:9090"
    env_file:
      - .env
    networks:
      - micro-net

networks:
  micro-net:
    external: true